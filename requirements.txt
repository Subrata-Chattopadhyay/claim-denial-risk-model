# Core ML / data stack (pinned to major versions used during development)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7

# Optional: only needed if you want to call a hosted LLM for the GenAI step.
# The pipeline runs fully offline without these (grounded template fallback).
# openai>=1.30
# anthropic>=0.30

# Dev / testing only.
pytest>=8.0
